{
  "name": "ez-docs-monorepo",
  "private": true,
  "scripts": {
    "dev": "turbo dev --filter=ez-docs-website",
    "build": "turbo build",
    "test": "turbo test",
    "check": "turbo check"
  },
  "devDependencies": {
    "turbo": "^2"
  }
}
